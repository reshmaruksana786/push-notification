# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'push notification 2' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for push notification 2

  target 'push notification 2Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'push notification 2UITests' do
    # Pods for testing
  end
pod 'Firebase/Messaging'

end
